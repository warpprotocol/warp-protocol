version: '3.8'
services:
  api:
    build: .
    ports:
      - '3000:3000'
    env_file: .env
    restart: unless-stopped
  inference:
    build:
      context: ./models/inference
    ports:
      - '8080:8080'
    restart: unless-stopped
